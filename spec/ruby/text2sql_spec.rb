# frozen_string_literal: true

RSpec.describe Ruby::Text2sql do
  it "has a version number" do
    expect(Ruby::Text2sql::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
